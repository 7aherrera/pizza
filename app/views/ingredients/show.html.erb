<p id="notice"><%= notice %></p>

<p>
  <strong>Vegetarian:</strong>
  <%= @ingredient.vegetarian %>
</p>

<p>
  <strong>Salty:</strong>
  <%= @ingredient.salty %>
</p>

<p>
  <strong>Sweet:</strong>
  <%= @ingredient.sweet %>
</p>

<!--this is code for an added gem on uploading images-->

<%= image_tag @ingredient.avatar.url %>
<%= image_tag @ingredient.avatar.url(:medium) %>
<%= image_tag @ingredient.avatar.url(:thumb) %>

<%= link_to 'Edit', edit_ingredient_path(@ingredient) %> |
<%= link_to 'Back', ingredients_path %>
